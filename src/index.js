import "./styles.css";

//Create a loop that logs each array element to the console.
//Hint: Remember, to log something to the console, you'll need to use console.log().;
let fruits = ["grape", "banana", "orange", "apple", "strawberry"];

fruits.map((fruit) => console.log(fruit));
